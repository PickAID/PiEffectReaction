/**
 * Optional bridges to sibling Pi runtime modules.
 *
 * <p>Bridges belong here so the public API does not leak implementation details
 * from Pibrary, PiDataGraph, PiEngine, or future visual packs.
 */
package org.pickaid.pieffectreaction.bridge;
